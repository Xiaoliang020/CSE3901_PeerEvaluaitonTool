<p id="notice"><%= notice %></p>

<h1>Student Groups</h1>

<table>
  <thead>
    <tr>
      <th>Group name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @student_groups.each do |student_group| %>
      <tr>
        <td><%= student_group.group_name %></td>
        <td><%= link_to 'Show', student_group %></td>
        <td><%= link_to 'Edit', edit_student_group_path(student_group) %></td>
        <td><%= link_to 'Destroy', student_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Student Group', new_student_group_path %>
<%= link_to "Back to Faculty Page", home_page_faculty_path %>
<%= link_to "Back to Home Page", home_page_home_path %>
